export interface RouteLink {
  url: string;
  title: string;
}

export interface AdditionalInformation {
  title: string;
  description: string;
  linkText: string;
  url: string;
}

export enum LANGS {
  pl = "pl",
  en = "en",
  is = "is"
}
